select tag, real_name, end_date3 from (select player2, max(end_date2) as end_date3 from (select player as player2, end_date as end_date2 from (select team_id from teams where name = "ROOT Gaming") honors inner join members on honors.team_id = members.team where members.end_date is not null and player not in (select player from (select team_id from teams where name = "ROOT Gaming") honors inner join members on honors.team_id = members.team where members.end_date is null)) honors2 group by player2) honors3 inner join players on honors3.player2 = players.player_id
